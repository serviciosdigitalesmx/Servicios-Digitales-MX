# backend-api

API inicial en ASP.NET Core para `Servicios Digitales MX`.

## Estado actual

- proyecto creado con `.NET 10`
- endpoint base de salud disponible en `/api/health`
- estructura preparada para crecer hacia:
  - `src/Api`
  - `src/Application`
  - `src/Domain`
  - `src/Infrastructure`

## Siguiente paso recomendado

- agregar auth base
- agregar contexto de tenant
- crear endpoints iniciales de `customers` y `service-orders`
